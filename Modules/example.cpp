#include <stdio.h>
#include <iostream>
using namespace std;

/*
* Example module writen in c++. Here you should put your binaries. Java will look for the "!a.exe/out" whent you send the command !a.
* It MUST start with ! to diferenciate from normal commands.
* To execute in Windows the extension should be ".exe".
* To execute in the rest of OS the extension should be ".out".
*/

int main()
{
    cout << "Example" << endl;
    return 0;
}